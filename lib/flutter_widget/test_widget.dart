import 'package:flutter/material.dart';

class TextWidgate extends StatelessWidget {
  const TextWidgate({Key? key}) : super(key: key);

  Widget build(BuildContext context) {
    return const Text('TRPL 2A : Belajar Mobile Dev');
  }
}
